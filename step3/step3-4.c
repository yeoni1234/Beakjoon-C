#include <stdio.h>


int main(){

    int sum;
    int type;
    scanf("%d %d", &sum, &type);

    for(int i = 0; i <= sum; i++){
        int price;  
        int count;
        scanf("%d %d", &price, &count);

    }


    return 0;

}